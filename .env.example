# Copy this file to .env.local for local dev. Real values go in Vercel's
# Project Settings → Environment Variables when deploying — never commit
# a filled-in .env.local or .env.

# Same Supabase project the existing Streamlit app already uses.
# Find both in Supabase dashboard → Project Settings → API.
NEXT_PUBLIC_SUPABASE_URL=https://taspskjeiesusxaobyqm.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_m7EjwibUn9BbZueur83M1w_QJN9XSzn



# Base URL of the FastAPI backend (backend/), once deployed to Render.
# Local dev: http://localhost:8000
# Production: https://your-service.onrender.com (or your api.* subdomain)
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000
